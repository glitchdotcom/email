extends layout.pug

block content
  a(href=baseDomain + path + "?token=" + joinToken)
    .button.button-cta Sign in to Glitch
  
  //section.secondary-info
  //  p Or, copy and paste this temporary sign in code:
  //  pre= joinToken

  aside
    p (If you didn't try to sign in, you can safely ignore this email)
